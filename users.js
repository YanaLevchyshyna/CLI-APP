const admins = ['Anna', 'Sophia', 'Yana', 'Olena'];

const clients = ['Ihor', 'Fedir', 'Roman'];

const users = {
  admins,
  clients,
};

// Таким чином експортуємо файл (відповідає експорт діфолт).
// Експорт на сторінці можу бути тільки один!!!
module.exports = users;
